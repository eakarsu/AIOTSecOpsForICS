{
  "name": "ai-ot-secops-for-ics",
  "version": "1.0.0",
  "description": "AI-powered OT/ICS security operations platform (SCADA/PLC/HMI assets, ICS incidents, vendor patches, network anomalies, safety systems)",
  "private": true,
  "scripts": {
    "start": "bash start.sh",
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && npm start",
    "seed": "cd backend && node seed/seed.js",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  }
}
